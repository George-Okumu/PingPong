import org.junit.Assert;
import org.junit.Test;

import static org.junit.Assert.*;

public class PongLineTest {
    @Test
    public void newPongLine_instantiatesCorrectly() {
        PongLine testPongLine = new PongLine();
        Assert.assertTrue(true);
    }
}